# AI Autopilot State

このファイルは **AI エージェント自身が更新する進捗メモ** です。  
人間が初期化しても構いませんが、以降は codex / claude がここを書き換えながら開発を進めます。

---

## ゴール

Go 言語でシンプルな REST API サーバーを実装し、  
`go test ./...` がすべて成功する状態まで持っていくこと。  
必要な README やサンプルリクエストも整備すること。

---

## 現在の状況

- `projects/sample-go-api` を新規作成し、メモリ内でタスクを管理するシンプルな REST API を実装。
- `GET /health`, `GET /tasks`, `POST /tasks`, `PATCH /tasks/{id}`, `PATCH /tasks/{id}/complete`, `DELETE /tasks/{id}` を提供。
- ハンドラとストアのユニットテストを追加し、`go test ./...` は成功。
- README を作成し、起動方法とサンプルリクエストを記載。
- `GET /tasks` に `offset` と `limit` を追加し、レスポンス量を抑えられるようにした。

---

## TODO

- [x] プロジェクトのディレクトリ構成を用意する
- [x] エントリポイントとなる `main.go` を作成する
- [x] ハンドラ/ストアのテストを追加し、`go test ./...` を通す
- [x] タスク完了以外の更新/削除エンドポイントを検討する（必要なら）
- [ ] 永続化やバリデーション強化など追加要件があれば対応する

---

## ログ

- 2025-12-18 08:57: `projects/sample-go-api` を初期化。タスク API を実装し、ヘルスチェックと CRUD 相当のエンドポイントを追加。ユニットテストが通ることを確認 (`go test ./...`)。README に使い方を追記。
- 2025-12-18 09:10: タスクのタイトル更新と削除エンドポイントを追加。テストと README を更新。
- 2025-12-18 09:20: 一覧取得に `offset` と `limit` を追加し、ページングでレスポンス量を削減できるようにした。
